<div>
  <p>Parent passes data to child slot:</p>
  <vuego include="list.vuego" :items="fruits">
    <template v-slot="{ item, index }">
      {{ index }}. {{ item }}
    </template>
  </vuego>
</div>
